const EtherWallet = artifacts.require('EtherWallet')

contract('EtherWallet', () => {

    before(async, () => {
        const EtherWallet = EtherWallet.deployed()
    });
    it('it should deposit ether', async () => {
        
    });
})